<nav class="p-4 flex gap-4 bg-slate-800">
    <a href="/">Home</a>
    <a href="{{ route('movies.index') }}">Filmovi</a>

    {% if auth_check() %}
        <a href="/home" class="ms-auto">Profil</a>
        <a href="/logout">Logout</a>
    {% else %}
        <a href="/login" class="ms-auto">Login</a>
    {% endif %}
</nav>
